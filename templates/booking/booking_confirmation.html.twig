{% extends 'base_booking.html.twig' %}

{% block title %}Reservation completed{% endblock %}

{% block content %}
    <div class="min-h-screen flex items-center justify-center pb-50">
        <div class="card bg-base-96  w-120 shadow-sm">
            <div class="card-body">
                <h2 class="card-title text-3xl font-bold mb-2">🎉 Congratulations!</h2>
                <h1 class="text-xl font-semibold mb-1">Reservation for <span
                        class="text-accent">{{ booking.festival.name }}</span> was successful!</h1>
                <h1 class="text-lg ">Reservation ID: {{ booking.id }}</h1>
                <a href="{{ path('app_booking_index') }}" class="btn btn-info">Back to list</a>
            </div>
        </div>
    </div>


{% endblock %}

